# Shadow Bounce config: forward_dynamics

agent:
  rollouts: 32
  learning_epochs: 4
  mini_batches: 64
  discount_factor: 0.99
  lambda: 0.95
  learning_rate: 1.5e-04
  ratio_clip: 0.2
  value_clip: 0.2
  entropy_loss_scale: 0
  value_loss_scale: 1.0
  kl_threshold: 0

ssl_task:
  type: forward_dynamics
  tactile_only: false
  loss_weight: 0.8462
  learning_rate: 4.53e-05
  n_rollouts: 1
  seq_length: 11

experiment:
  experiment_name: forward_dynamics
  wandb: 1
  wandb_kwargs:
    group: forward_dynamics
    name: orca bounce dynamics
